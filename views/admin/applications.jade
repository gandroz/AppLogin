div.row-fluid 
   div(ng-init="initApplications()")  
      accordion.span8.offset1(close-others="false")
         accordion-group(ng-repeat="item in data.applications", heading="{{$index}}")
            accordion-heading
               {{$index}}
               i.pull-right.icon-remove-circle.removeButton(ng:click="removeApplication(item)") 
            {{item}}